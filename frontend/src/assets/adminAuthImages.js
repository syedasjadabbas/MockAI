// Centralized photography reference for the Admin authentication screen
// (pages/AdminLogin.jsx). One deliberate image, not a gallery - a real,
// verified (HTTP 200 + visually inspected) black-and-white double-exposure
// photograph of professionals at work, chosen because its monochrome,
// layered quality takes a duotone wine treatment (see .aa-duotone in
// admin-auth-theme.css) well, making it read as MockAI's own photography
// rather than dropped-in stock art - deliberately distinct from both the
// Candidate Panel's warm portrait photography and the previous, more
// generic architectural shot this replaces.
export const ADMIN_AUTH_HERO =
  'https://images.unsplash.com/photo-1553877522-43269d4ea984?auto=format&fit=crop&w=1600&q=80';
